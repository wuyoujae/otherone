import { ModuleManifest } from '../plugin.types';

const craftManifest: ModuleManifest = {
  id: 'craft',
  icon: 'Sparkles',
  titleKey: 'plugins.craftTitle',
  descKey: 'plugins.craftDesc',
  primary: true,
  routePath: 'craft',
  order: 1,
};

export default craftManifest;
